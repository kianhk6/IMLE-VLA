# clip 2 — v8 notes (Fable, after the PACING review)   start from v7; do not edit v7

Re-timing only; no text changes.
1. `mean` beat: the links arrive at +2.30 s instead of +1.30 s (the picture was finished 3.5 s
   before its caption left). No length change.
2. Closing, series rule (clip 1's style): the LAST caption stays up through the 1.00 s closing hold
   and the 0.50 s fade to black (v7 drops it first, then holds). Adjust the tail so the total
   length changes by at most the removed caption fade-out; report the new total.
Deliver v8 with --stills; do not render.
