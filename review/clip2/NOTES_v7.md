# clip 2 — v7 notes (Fable, after SERIES review 3)   start from v6; do not edit v6

1. A1 caption: "Last clip: one observation, two valid chunks. Here, a 2-D toy with four."
   ("2-D", the form clip 3 uses for "1-D"/"2-D"). Same word count, no hold change.
2. Headline timing: "The simplest action head: regression" now holds over the reg AND feat beats;
   "Regression averages the modes" enters at the start of the conv beat (the convergence
   animation), using the existing fade-out / empty band / fade-in swap. The conclusion then
   arrives after the two beats that argue for it, not before.
Everything else as v6. Deliver v7 with --stills; do not render.
