# clip 2 — v3 notes (Fable, after v2 review)   start from v2; do not edit v2

1. Mean beat (partA mean): the white X marker is drawn on top of the neutral star → a smudge.
   Fade the neutral star out over 0.3 s as the X marker fades in (alpha/re-timing only), and
   stop the four dashed links ~12 px short of the marker.
2. Keep the 'feat' tag "near a valid mode, still penalized" (the author's own picture label);
   DECLINE hiding it.
3. Caption 2 stays "Four pairs, one colour each: the same observation, four different actions."
4. Everything else in v2 stands (one-panel sim page, headline cross-fade, 150 wpm).
Deliver v3 with --stills; do not render.
