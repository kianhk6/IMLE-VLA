# clip 3 — v8 notes (Fable, after SERIES review 3)   start from v7; do not edit v7

Hard limit stays <= 85.0 s (v7 is 84.13 s; nothing below adds words).

1. 3.1 row: the ten blocks read "Action head" (not "Action expert"), and the brace reads
   "×10  forward passes on the action head". One component, one name: clip 1 defined
   "action head" and this row's own label says "flow-matching action head".
2. 3.1 chip 3: "Stop-and-go: after a chunk is executed, the robot waits for the VLA"
   ("policy" is never defined in the series).
3. 3.2 c2: "But the flow is invertible and keeps dimension: 2-D noise stays 2-D."  (12 words;
   the headline and the tag already say "off the manifold" on the same frame, so the caption
   carries only the reason).
4. 3.2: the "off the manifold: jerky" rings and leader lines enter only AFTER the morph has
   completed (v7: 0.6 s before it ends).
5. 3.3 title sub-line: "same backbone, same tasks; only the action head differs"
   (tells the viewer what the three videos test; "ours" is otherwise unexplained until clip 4).
6. 3.3c left pill: "Done" (drop "· 2.5x sooner": under the JERK headline the speed claim
   repeats 3.3b's, and the headline's 2.7x is this beat's number).
7. Assertion: every caption plate's x extent inside 96..1824 (state it explicitly; the 1618 px
   line rule is already in place).
Everything else as v7. Deliver v8 with --stills; do not render.
