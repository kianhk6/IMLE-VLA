# clip 3 — v14 notes (Fable, after checking v13's pills at full size)   start from v13; do not edit v13

1. 3.3a-c result pills: keep Medium 52 but height 90 (v13's h 78 leaves ~6 px above the cap
   height and below the descenders, so "Placed on the moving plate" nearly touches the outline).
   Vertical pad >= 14 px measured from the cap top and from the descender bottom; horizontal pads
   unchanged. Nothing else moves; if a taller pill now sits < 12 px from the panel's top edge or
   from the "2x speed" tag, shift it by the minimum and report the px.
2. Record in LEGIBILITY_v1.md's spirit: status chips are h 78 for single-line text without
   descenders (clips 1, 2, 4: "success", "task failed"); clip 3's pills carry a check glyph and
   descenders, hence h 90.
Everything else as v13 (total 84.33 s). Deliver v14 with --stills; do not render.
