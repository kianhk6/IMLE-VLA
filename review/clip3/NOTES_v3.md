# clip 3 — v3 notes (Fable, after v2 review)   start from v2; do not edit v2

1. 3.2 headline: "And it is jerky: samples land beside the data" (one headline for the whole
   segment; it closes the argument the captions leave open). Not "Part of why:".
2. 3.2 caption box: its bottom sits at y 1076, 4 px from the frame edge. Match the other
   segments' box: bottom ≤ 1044 (reduce the flow section's box padding / leading; the
   two-line captions must still fit ≤ 1728 px).
3. 3.2: the "data manifold" label pill sits on the curve and hides two sample dots; move it to
   the empty area below-left of the curve (short leader allowed, same pill style).
4. Seams into 3.3a / 3.3b / 3.3c: the headline and category label must fade in on the same
   frame as the panels appear after the dip (now 4-6 frames late; the band is empty while the
   panels are up).
5. Keep: the stacked three chips of 3.1, chip 2 "by the time a chunk is ready, its observation
   is stale", "3B params", the title card at 5.4 s, no gripper trail, fitted caption widths
   (DECLINE the fixed-width box), the noise inset top at 208 (DECLINE).
Deliver v3 with --stills; do not render.
