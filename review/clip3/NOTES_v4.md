# clip 3 — v4 notes (Fable, after v3 review)   start from v3; do not edit v3

1. 3.2 headline in two states, swapped with the 6-frame fade-out / fade-in used elsewhere:
   "And it is jerky. Why?" while the bare curve is up (captions i1, i2), then
   "Samples land just beside the data" from caption c1 on (the trained-field animation), so
   the conclusion never precedes its picture. Never both on screen.
2. 3.2 caption c2: "But the flow is invertible, so it cannot: the samples stay just off the
   manifold." ("the flow", as c1 says; hold unchanged).
3. 3.2: move the "noise space" label INSIDE the noise panel as a top-left pill in the same
   style as the "ambient space" pill (it now sits in the headline band, 11 px under the
   headline). Re-labelling/placement only.
4. 3.3a (moving plate): end the segment 0.8 s after its last pill is fully drawn
   (react out at 60.8 s, −24 frames); nothing else moves.
Keep everything else as v3.  Deliver v4 with --stills; do not render.
