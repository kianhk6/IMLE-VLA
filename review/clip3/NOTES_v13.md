# clip 3 — v13 notes (Fable, after RATING panel 2)   start from v12; do not edit v12

Ceiling 85.0 s (v12 is 84.73 s; item 3 removes a word, -0.40 s).
1. 3.3c footnote: "jerk = third finite difference of joint positions over dt³, averaged over
   joints and steps" (the paper's definition normalises by dt³; the two systems run at different
   rates, so the ratio depends on it). Same size and ink as v12.
2. 3.3c contrast line, same slot and style as 3.3a/3.3b (green half · coral half, Regular 50):
   "IMLE-VLA moves smoothly · π0.5 moves in jolts", visible from the videos' start until the
   75.8 s swap, when the range line and footnote replace it as now.
3. 3.2 i2: "Thin: they span few ambient dimensions; | here, a curve in a plane."  (12 words; with
   "span" the marked break's shorter line is >= 55 % of the longer, so the "|" break at the
   semicolon is honoured; if it still is not, report the ratio).
Everything else as v12. Deliver v13 with --stills; do not render. Report the total.

ADDENDUM (editor re-rating), apply in v13 if still building, else as v14:
4. 3.3a-c: result pills Medium 52 / h 78; name plates ("IMLE-VLA", "π0.5") Medium 48 / h 70;
   footnote Regular 42 GREY — LEGIBILITY_v1's sizes exactly, so clip 3 matches clips 1, 2 and 4
   (v12 kept them at 46 / 50 / 40). Widest pill grows to ~652 px inside the 900 px panel.
