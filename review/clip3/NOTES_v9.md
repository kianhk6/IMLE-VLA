# clip 3 — v9 notes (Fable, after the PACING review)   start from v8; do not edit v8

Re-timing only; no text changes; total stays <= 85.0 s (v8 is 83.33 s).
1. 3.3c: the headline swap to "2.7x lower jerk than pi0.5", the JERK category label and the
   footnote all move from 73.6/73.85 s to 75.8 s, i.e. AFTER the right panel's "Done" pill lands
   (75.77 s), so the text change no longer competes with the only motion on screen. The segment's
   end stays where it is (the footnote still gets ~6.0 s, its reading time).
2. 3.1 chips: enter at 3.6 / 8.6 / 13.8 s (gaps 5.0 / 5.2 s, tail 5.4 s) instead of 3.6 / 7.6 /
   13.6, so the cadence is even. No length change.
Deliver v9 with --stills; do not render.
