# clip 4 — v14 notes (Fable, after RATING panel 2)   start from v13; do not edit v13

1. 4.1 stat block line 2: "in simulation, executing 3x more of each chunk" (the 11x / 3x figures
   are the LIBERO H = 30 result; the chips beside them are the real-robot runs at H = 12 vs 8, so
   the line must say which setting it belongs to). Line 1 unchanged. If the two lines' reading
   time now exceeds the block's slot before 13.0 s, extend 4.1 by the minimum and report it.
Everything else as v13. Deliver v14 with --stills; do not render.

ADDENDUM (editor re-rating), apply in v14 if still building, else as v15:
2. 4.3: the phrase under the objective changes by the series band rule: 6 frames out to an empty
   phrase slot, then 6 frames in (0.40 s total), at every beat change; never two phrases
   superimposed. The equation's lit-state cross-fade (same glyphs) stays as is.
3. 4.4 `left`: LEFT_HOLD_F 60 -> 30 (the caption then arrives 1.0 s earlier and the rollout starts
   1.0 s earlier; chip and `both` timing follow unchanged; clip ~93.2 s).
