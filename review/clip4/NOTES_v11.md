# clip 4 — v11 notes (Fable, after the PACING review)   start from v10; do not edit v10

Re-timing only; no text changes. PLAN's 100-125 s window is AMENDED to 90-125 s for this clip:
the pacing review measured 21 s of near-static pages between 24 and 45 s, and removing dead air
beats the floor. Update the length assertion to 90-125.

1. 4.2a `direct` hold after caption 2's box leaves: 2.00 -> 0.30 s (clips 1 and 2 cut 0.15 s after
   the last caption; this matches them).
2. 4.3 `intro`: hold 3.60 -> 2.00 s ("the cIMLE objective" is 3 words: 2.0 s by the rule);
   `draw`'s first event at +0.40 s instead of +0.90.
3. 4.2b: the right (IMLE) panel and its pill enter at +1.5 s instead of at 0, so the page has
   one motion; the caption timing is unchanged; no length change.
4. 4.3 `free`: events at +0.8 / +1.4 / +2.25 s (were +2.2 / +2.8 / +3.65); beat 6.30 -> 5.00 s,
   the in-panel note keeps its 3.2 s. `cover`: rings from +1.2 s (was +2.8); beat 6.32 -> 5.00 s.
5. 4.3 `compare` hold 4.50 -> 3.60 s (the phrase is 6 words: 3.2 s by the rule, +0.4 s settle).
6. Closing, series rule (clip 1's style): the LAST caption ("One forward pass per chunk, and every
   mode kept.") stays up through a 1.00 s closing hold (was 0.70) and the 0.50 s fade to black;
   it no longer fades out first.
Expected total about 94 s. Deliver v11 with --stills; report the new total per segment.
