# clip 4 — v3 notes (Fable, after v2 review)   start from v2; do not edit v2

1. Kicker on every frame: "IMLE-VLA · 4 / 4 · ONE PASS, EVERY MODE" ("step" = one action
   in clip 1). 4.4 right caption: "One pass, one mode: the cIMLE sample commits to a bottle
   and places it." 4.4 left caption: "From clip 2: three modes; regression closed on the
   empty plate." (hold by the rule).
2. 4.2a layout: centre the whole group (noise inset + curve panel) horizontally between the
   50 px gutters — translate by one constant, nothing rescaled; keep both panel tops level.
   The "one data point" pill must not cover a data dot: offset it left of the curve with a
   short leader.
3. 4.2b closer: draw its two coloured lines inside the standard caption box (same y band and
   corner radius as 4.2a's box; keep the coral/green ink). Give the "π0.5 · flow matching"
   pill an opaque panel-fill backing with a few px padding so the yellow dots do not show
   through it.
4. 4.3: widen the equation plate to the panel columns' edges (x 97..1823) so the edges line
   up. Keep the intro phrase "the cIMLE objective" (DECLINE "m = 1 is regression").
5. 4.4: show the same base pills ("simulation", "sped up") on both panels from the first
   frame; only the status pill ("task failed" / "success") comes with each video.
6. 4.1: distribute the three chips over the panel's width (x 50..1870), so the last chip's
   right edge lands on the panel's right edge.
Deliver v3 with --stills; do not render.
