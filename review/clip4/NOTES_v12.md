# clip 4 — v12 notes (Fable, after the RATING panel)   start from v11; do not edit v11

Re-timing and one seam fix; no text changes. Window 90-125 s.
1. Seam 4.2a > 4.2b: the headline is the same on both sides, so the dip must NOT dim the headline
   band (y 110..250), exactly as the kicker rows are excluded; only y >= 254 dips. (Measured v11:
   band max 241 -> 49 -> 241 across the seam.) Keep the 4.2b > 4.3 dip as is (the headline changes).
2. 4.4 `right`: playback starts at +54 f instead of +24 f, so the "success" chip lands 1.0 s
   later and the static tail before `both` drops from 3.4 s to 2.4 s; beat length unchanged.
3. 4.4 `both`: the beat holds 5.0 s (9-word caption: 4.4 s rule + 0.6 s), then the 1.00 s hold and
   the 0.50 s fade with the caption kept up (v11: ~7 s on a frozen frame).
Deliver v12 with --stills; report the total and the seam's band values.
