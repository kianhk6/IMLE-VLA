# clip 1 — v5 notes (Fable, after SERIES review 2)   start from v4; do not edit v4

Series-wide vocabulary: "sample" = the generator's output, "z" = the noise, "pair" = (observation,
action chunk), "mode" = one valid way of doing the task, "action head" = the part of the VLA that
outputs the chunk. Clip 1 must introduce "action head" and "mode" because clips 2-4 use them.

1. A4 caption: "The VLA's action head maps that observation to an action chunk."  (11 words)
2. B2 caption: "Same observation, two valid action chunks: two modes of a multimodal distribution."  (12)
   (keeps the author's ending "a multimodal distribution"; "where the routes split" is what the
   two-route footage and B1's headline already show).
3. A7: do NOT draw the in-panel tag "one observation -> one action chunk" (38.97 s); the caption
   "Each observation, the condition, is paired with one action chunk, the data." says it. Nothing
   else in A7 moves; keep A7's length.
4. Fold in your own pending storyboard string fix (the "2x speed" tags at play_in).
Holds follow the 150 wpm rule as before; length stays inside 45-55 s. Deliver v5 with --stills;
do not render.
