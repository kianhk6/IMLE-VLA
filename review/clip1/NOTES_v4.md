# clip 1 — v4 notes (Fable, after v3 review)   start from v3; do not edit v3

Both items are re-timing only; no wording changes.
1. A2 (beat_chunk): the frozen tail is 2.15 s. Thumbnails t+1..t+5 at 0.90 + (i-1)*0.30 (0.40 s
   each, last lands +2.10), columns and ticks from 2.60 + (k-1)*0.25, the label / rule at
   +4.30 (0.45 s), anim 4.15 -> 4.75; update ELEMENTS['chunk']. Hold stays 6.00 s.
2. A2 -> A3 morph (cut_strip_headswap1, f396-f398): the "C steps x D dims" sublabel is still
   fading while the grid slides right, so for ~3 frames its ghost sits on the t+5 column.
   Fade that sublabel (and "action chunk A" if it moves with it) out together with the
   headline, before the grid starts moving, or lock it to the grid's translation.
Declined (keep v3): re-sourcing the B2 observation card from the split frame; hiding the
"same observation" chip; the B2 caption stays as v3.
Deliver v4 with --stills; do not render.
