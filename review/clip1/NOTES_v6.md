# clip 1 — v6 notes (Fable, after the v5 build report)   start from v5; do not edit v5

1. A4 caption: "The VLA's action head maps the observation to an action chunk."  (10 words; A3 has
   just defined "the observation", so "that" is not needed). Total lands at 54.967 s, inside 45-55.
2. A4 re-timing at zero cost: spread the box, the input arrow and the output arrow across the
   hold the way A1, A2 and A5 already are, so the settled tail is ~1.5 s, not 2.45 s.
3. Restore the length assertion (45-55 s) as a hard check now that it holds again; keep the
   TARGET MISS menu in the report for the future.
Everything else as v5. Deliver v6 with --stills; do not render.
